def VowelCount(str): 
  
	vowels = ['a', 'e', 'i', 'o', 'u']
	vowels_in_str = []

	count = 0
	
	for x in str:
		for y in vowels:
			if x == y:
				count += 1
				vowels_in_str = vowels_in_str + [y]

	print len(vowels_in_str)
	
usertxt = raw_input("Some random text: ")
	
VowelCount(usertxt) 