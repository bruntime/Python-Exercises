def LetterCount(txt):

	characters = list(txt)
	characters.sort()
	print characters

	for x in characters:
		if characters[a] == characters[b]:
			
user_txt = raw_input("word: ")

LetterCount(user_txt)





